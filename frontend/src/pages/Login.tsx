import{useState}from'react';import{api}from'../api';
export default function Login({done}:{done:()=>void}){const[email,setEmail]=useState('admin@demo.com'),[password,setPassword]=useState('Admin@123'),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await api<any>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',r.access_token);localStorage.setItem('refresh_token',r.refresh_token);localStorage.setItem('user',JSON.stringify(r.user));done()}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <main className="login"><section className="login-card"><div className="brandmark">N</div><h1>Bem-vindo ao NovaFlow</h1><p>Gestão inteligente de serviços e atendimentos.</p><form onSubmit={submit}><label>E-mail<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Senha<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'Entrando…':'Entrar na plataforma'}</button></form><small>Acesso demo: admin@demo.com · Admin@123</small></section></main>}

