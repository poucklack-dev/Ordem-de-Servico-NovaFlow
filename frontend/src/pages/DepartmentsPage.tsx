import{useEffect,useState}from'react';import{api}from'../api';import{Plus}from'lucide-react';import{Empty,Modal,Notice,Title}from'../components/UI';
export default function DepartmentsPage(){const[rows,setRows]=useState<any[]>([]),[open,setOpen]=useState(false),[message,setMessage]=useState('');const load=()=>api<any>('/catalog').then(x=>setRows(x.departments));useEffect(()=>{void load()},[]);async function save(e:any){e.preventDefault();const name=new FormData(e.currentTarget).get('name');await api(`/departments?name=${encodeURIComponent(String(name))}`,{method:'POST'});setOpen(false);setMessage('Setor criado com sucesso.');load()}return <><Title title="Setores" sub="Estrutura organizacional da operação" action={<button className="primary" onClick={()=>setOpen(true)}><Plus size={17}/>Novo setor</button>}/><Notice message={message}/><div className="panel table-panel"><table><thead><tr><th>Setor</th><th>Identificador</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>#{x.id}</td></tr>)}</tbody></table>{!rows.length&&<Empty text="Nenhum setor cadastrado"/>}</div>{open&&<Modal title="Novo setor" onClose={()=>setOpen(false)}><form className="form" onSubmit={save}><label>Nome<input name="name" required/></label><button className="primary">Salvar</button></form></Modal>}</>}
